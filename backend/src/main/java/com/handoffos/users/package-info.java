/**
 * Agency users, roles (ACCOUNT_OWNER, ADMIN, PROJECT_MANAGER, TEAM_MEMBER, CLIENT), membership.
 *
 * Empty for now. Follow the same layout as the clients package.
 */
package com.handoffos.users;
